mixin book()
    - for (var d of result)
        tr
            td= d.name
            td= d.phone
            td
                ul
                    li
                        b skype:
                        |  simplesten


doctype html
html(lang="ru")
    head
        title= pageTitle
        link(rel="stylesheet", href="//netdna.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css")
        style.
            body{padding: 30px}
            .clear{clear:both}
            span.input-group-addon{font-size: 20px;color:red;}
    body
        h4 Добавить в записную книжку
        form(role="form", method='post' action='api').col-sm-4
            label Имя
            .input-group
                span.input-group-addon ✩
                input(type='text', name='name', required).form-control
            hr
            label Телефон
            .input-group
                span.input-group-addon ✩
                input(type='tel', name='phone', required).form-control
            hr
            label Дополнительные параметры
            select(name='attr').form-control
                option(selected, disabled) Не выбрано
                option(value='skype') skype
                option(value='vk') vk
                option(value='facebook') facebook
            br
            input(name='attrVal', type='text',disabled).form-control
            br
            input(type='submit').btn.btn-primary
        div.clear
        br
        br
        h3 Записная книжка
        table.table.table-bordered
            tr.active
                th Имя
                th Телефон
                th Дополнительные параметры
            +book()

